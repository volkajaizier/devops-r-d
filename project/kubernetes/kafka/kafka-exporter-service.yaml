apiVersion: v1
kind: Service
metadata:
  name: kafka-exporter 
  namespace: prod
  labels:
    app.kubernetes.io/name: kafka-exporter
spec:
  ports:
  - port: 9404 
    targetPort: 9404
  selector:
    app.kubernetes.io/name: kafka-exporter
